#include <iostream>
#include <string>
#include "Character.h"

using namespace std;

string* opponent_array(Character user, int* random_array)
// This function will return an array of Character names in random order.
{	
	string* opponents = new string[4];

	return opponents;
}
